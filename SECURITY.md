# Security

This example creates a high-authority application in your Cloudflare account. Use a separate account until the live installation and removal paths are verified.

Do not commit `.env`, `vendor/`, Alchemy state, Cloudflare credentials, Access tokens, or Executor credentials.

Report vulnerabilities privately through a GitHub Security Advisory on the repository. Do not include live credentials or sensitive deployment output.
